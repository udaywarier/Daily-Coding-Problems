function num_ways_decoded(str)
{
    if(str.length === 0)
    {
        return 0;
    }

    else if(str.length === 1)
    {
        return (parseInt(str, 10) != 0) ? 1 : 0;
    }

    else
    {
        
    }
}